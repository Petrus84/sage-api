# 🔧 CONFIGURAÇÃO SAGE-NFM API

# MongoDB (Obrigatório)
MONGODB_URI=mongodb+srv://usuario:senha@cluster.mongodb.net/sage-nfm?retryWrites=true&w=majority

# Redis (Opcional - para cache)
REDIS_URL=redis://default:senha@redis-host:6379

# JWT Secret (Obrigatório)
JWT_SECRET=seu-jwt-secret-super-seguro-aqui

# Ambiente
NODE_ENV=production
PORT=3000

# ⚠️ IMPORTANTE:
# 1. Copie este arquivo para .env.local
# 2. Substitua os valores pelos seus dados reais
# 3. NUNCA commite .env.local no Git
# 4. Configure as mesmas variáveis no Vercel Dashboard
